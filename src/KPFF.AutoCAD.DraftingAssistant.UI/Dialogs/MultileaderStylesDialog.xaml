<Window x:Class="KPFF.AutoCAD.DraftingAssistant.UI.Dialogs.MultileaderStylesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit Multileader Styles"
        Height="350"
        Width="500"
        WindowStartupLocation="CenterOwner"
        Background="#2E3440">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Edit Multileader Styles" 
                   Style="{StaticResource DarkTextBlockStyle}"
                   FontSize="14" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>
        
        <!-- Instructions -->
        <TextBlock Grid.Row="1" 
                   Text="Enter multileader style names separated by commas (e.g., ML-STYLE-01, ML-STYLE-02, ML-STYLE-03):" 
                   Style="{StaticResource DarkTextBlockStyle}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>
        
        <!-- Text Area -->
        <Border Grid.Row="2" 
                BorderBrush="{StaticResource DarkBorderBrush}" 
                BorderThickness="1" 
                CornerRadius="3" 
                Background="{StaticResource DarkSurfaceBrush}">
            <TextBox Name="StylesTextBox" 
                     Style="{StaticResource DarkTextBoxStyle}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Padding="10"
                     FontFamily="Consolas"
                     BorderThickness="0"
                     Background="Transparent"/>
        </Border>
        
        <!-- Dialog Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Name="OkButton" 
                    Content="OK" 
                    Width="80" 
                    Height="30"
                    Style="{StaticResource DarkButtonStyle}"
                    Click="OkButton_Click"
                    IsDefault="True"
                    Margin="0,0,10,0"/>
            <Button Name="CancelButton" 
                    Content="Cancel" 
                    Width="80" 
                    Height="30"
                    Style="{StaticResource DarkButtonStyle}"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>