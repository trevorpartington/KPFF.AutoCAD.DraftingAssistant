<Window x:Class="KPFF.AutoCAD.DraftingAssistant.UI.Dialogs.SeriesSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Select Series"
        Height="400"
        Width="300"
        WindowStartupLocation="CenterOwner"
        Background="#2E3440">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Select Drawing Series" 
                   Style="{StaticResource DarkTextBlockStyle}"
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,15"/>
        
        <!-- Series List -->
        <Border Grid.Row="1" 
                Style="{StaticResource DarkBorderStyle}"
                BorderThickness="1" 
                CornerRadius="5"
                Background="{StaticResource DarkSurfaceBrush}">
            <ScrollViewer>
                <ItemsControl x:Name="SeriesItemsControl" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Margin="0,5,0,5"
                                      IsChecked="{Binding IsSelected}"
                                      Style="{StaticResource DarkCheckBoxStyle}">
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding SeriesName}" 
                                              FontWeight="SemiBold" 
                                              Style="{StaticResource DarkTextBlockStyle}"/>
                                    <TextBlock Text="{Binding SheetCountText}" 
                                              FontSize="11" 
                                              Opacity="0.8"
                                              Style="{StaticResource DarkTextBlockStyle}"/>
                                </StackPanel>
                            </CheckBox>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
        
        <!-- Selection Controls -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Left" 
                    Margin="0,15,0,15">
            <Button Name="SelectAllButton" 
                    Content="Select All" 
                    Width="80" 
                    Height="30"
                    Style="{StaticResource DarkButtonStyle}"
                    Click="SelectAllButton_Click"
                    Margin="0,0,10,0"/>
            <Button Name="ClearAllButton" 
                    Content="Clear All" 
                    Width="80" 
                    Height="30"
                    Style="{StaticResource DarkButtonStyle}"
                    Click="ClearAllButton_Click"/>
        </StackPanel>
        
        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Name="OkButton" 
                    Content="OK" 
                    Width="80" 
                    Height="30"
                    Style="{StaticResource DarkButtonStyle}"
                    Click="OkButton_Click"
                    IsDefault="True"
                    Margin="0,0,10,0"/>
            <Button Name="CancelButton" 
                    Content="Cancel" 
                    Width="80" 
                    Height="30"
                    Style="{StaticResource DarkButtonStyle}"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>