<Window x:Class="KPFF.AutoCAD.DraftingAssistant.UI.Dialogs.NoteBlocksDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit Note Blocks"
        Height="450"
        Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#2E3440">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Edit Note Blocks" 
                   Style="{StaticResource DarkTextBlockStyle}"
                   FontSize="14" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>
        
        <!-- Instructions -->
        <TextBlock Grid.Row="1" 
                   Text="Configure block references that contain construction note numbers. Each block configuration requires a block name and the attribute name that contains the note number." 
                   Style="{StaticResource DarkTextBlockStyle}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>
        
        <!-- DataGrid and Buttons -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- DataGrid for Block Configurations -->
            <Border Grid.Column="0"
                    BorderBrush="{StaticResource DarkBorderBrush}" 
                    BorderThickness="1" 
                    CornerRadius="3" 
                    Background="{StaticResource DarkSurfaceBrush}">
                <DataGrid Name="BlocksDataGrid" 
                          AutoGenerateColumns="False"
                          CanUserAddRows="True"
                          CanUserDeleteRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          Background="{StaticResource DarkSurfaceBrush}"
                          Foreground="{StaticResource DarkTextBrush}"
                          BorderThickness="0"
                          RowBackground="{StaticResource DarkSurfaceBrush}"
                          AlternatingRowBackground="{StaticResource DarkBackgroundBrush}"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Block Name" 
                                            Binding="{Binding BlockName}" 
                                            Width="*" 
                                            MinWidth="150"/>
                        <DataGridTextColumn Header="Attribute Name" 
                                            Binding="{Binding AttributeName}" 
                                            Width="*" 
                                            MinWidth="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            
            <!-- Action Buttons -->
            <StackPanel Grid.Column="1" 
                        Orientation="Vertical" 
                        Margin="10,0,0,0"
                        VerticalAlignment="Top">
                <Button Name="DeleteButton" 
                        Content="Delete" 
                        Width="80" 
                        Height="30"
                        Style="{StaticResource DarkButtonStyle}"
                        Click="DeleteButton_Click"/>
            </StackPanel>
        </Grid>
        
        <!-- Instructions for DataGrid -->
        <TextBlock Grid.Row="3" 
                   Text="To add new items: Click in the empty row at the bottom and enter values. Examples: Block Name = '_TagCircle', Attribute Name = 'TAGNUMBER'" 
                   Style="{StaticResource DarkTextBlockStyle}"
                   FontStyle="Italic"
                   FontSize="11"
                   TextWrapping="Wrap"
                   Margin="0,10,0,0"/>
        
        <!-- Dialog Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Name="OkButton" 
                    Content="OK" 
                    Width="80" 
                    Height="30"
                    Style="{StaticResource DarkButtonStyle}"
                    Click="OkButton_Click"
                    IsDefault="True"
                    Margin="0,0,10,0"/>
            <Button Name="CancelButton" 
                    Content="Cancel" 
                    Width="80" 
                    Height="30"
                    Style="{StaticResource DarkButtonStyle}"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>