<local:BaseUserControl x:Class="KPFF.AutoCAD.DraftingAssistant.UI.Controls.PlottingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KPFF.AutoCAD.DraftingAssistant.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#2E3440">
    <local:BaseUserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </local:BaseUserControl.Resources>
    
    <Grid Background="{StaticResource DarkBackgroundBrush}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Controls -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Checkboxes -->
            <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,15">
                <CheckBox Name="UpdateConstructionNotesCheckBox" 
                          Content="Update construction notes before plotting" 
                          Style="{StaticResource DarkCheckBoxStyle}"
                          HorizontalAlignment="Left"
                          Margin="0,0,0,5"
                          IsChecked="True"/>
                
                <CheckBox Name="UpdateTitleBlocksCheckBox" 
                          Content="Update title blocks before plotting" 
                          Style="{StaticResource DarkCheckBoxStyle}"
                          HorizontalAlignment="Left"
                          Margin="0,0,0,5"
                          IsChecked="True"/>
                
                <CheckBox Name="ApplyToCurrentSheetCheckBox" 
                          Content="Apply to current sheet only" 
                          Style="{StaticResource DarkCheckBoxStyle}"
                          HorizontalAlignment="Left"
                          Margin="0,5,0,0"/>
            </StackPanel>
            
            <!-- Button -->
            <StackPanel Grid.Row="1" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Left">
                <Button Name="PlotButton" 
                        Content="Plot Selected Sheets" 
                        Width="150" 
                        Height="30"
                        Style="{StaticResource DarkButtonStyle}"
                        Click="PlotButton_Click"/>
            </StackPanel>
        </Grid>
        
        <!-- Text Readout Area -->
        <Border Grid.Row="1" 
                Style="{StaticResource DarkBorderStyle}"
                BorderThickness="1" 
                CornerRadius="5"
                Background="{StaticResource DarkSurfaceBrush}">
            <ScrollViewer Margin="15" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="PlottingTextBlock"
                          Text="Plotting System\n\nReady to plot selected sheets to PDF using their default page setup.\n\nOptions:\n• Update construction notes before plotting\n• Update title blocks before plotting\n• Apply operations to current sheet only\n\nOutput files will be saved as {SheetName}.pdf in the configured output directory."
                          Style="{StaticResource DarkTextBlockStyle}"
                          TextWrapping="Wrap"
                          FontFamily="Consolas"
                          FontSize="12"
                          LineHeight="18"/>
            </ScrollViewer>
        </Border>
    </Grid>
</local:BaseUserControl>