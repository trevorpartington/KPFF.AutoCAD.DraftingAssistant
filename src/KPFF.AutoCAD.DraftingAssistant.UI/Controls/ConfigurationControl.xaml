<local:BaseUserControl x:Class="KPFF.AutoCAD.DraftingAssistant.UI.Controls.ConfigurationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KPFF.AutoCAD.DraftingAssistant.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#2E3440">
    <local:BaseUserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </local:BaseUserControl.Resources>
    
    <Grid Background="{StaticResource DarkBackgroundBrush}" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Button Row -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Action Buttons -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" 
                        Name="SelectSheetsButton" 
                        Content="Sheets" 
                        Width="100" 
                        Height="30"
                        Style="{StaticResource DarkButtonStyle}"
                        Click="SelectSheetsButton_Click"/>
                <Button Grid.Column="2" 
                        Name="SelectProjectButton" 
                        Content="Project" 
                        Width="100" 
                        Height="30"
                        Style="{StaticResource DarkButtonStyle}"
                        Click="SelectProjectButton_Click"/>
            </Grid>
            
            <!-- Active Project Status -->
            <StackPanel Grid.Row="1" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Left">
                <TextBlock Text="Active Project: " 
                          Style="{StaticResource DarkTextBlockStyle}"
                          FontWeight="SemiBold"
                          VerticalAlignment="Center"/>
                <TextBlock Name="ActiveProjectTextBlock" 
                          Text="No project selected" 
                          Style="{StaticResource DarkTextBlockStyle}"
                          FontStyle="Italic"
                          VerticalAlignment="Center"
                          Foreground="White"/>
            </StackPanel>
            
        </Grid>
        
        <!-- Text Readout Area -->
        <Border Grid.Row="1" 
                Style="{StaticResource DarkBorderStyle}"
                BorderThickness="1" 
                CornerRadius="5"
                Background="{StaticResource DarkSurfaceBrush}">
            <ScrollViewer Margin="15" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ConfigurationTextBlock"
                          Text="Configuration Management

Select a project configuration file to get started:
• Click 'Select Project' to browse for a ProjectConfig.json file
• Use 'Configure Project' to modify project settings
• Choose 'Select Sheets' to pick which layouts to process

Once configured, project details and sheet information will appear here."
                          Style="{StaticResource DarkTextBlockStyle}"
                          TextWrapping="Wrap"
                          FontFamily="Consolas"
                          FontSize="12"
                          LineHeight="18"/>
            </ScrollViewer>
        </Border>
    </Grid>
</local:BaseUserControl>